---
import { typeColor } from "../lib/typeColors";
import { typeNameById } from "../lib/data";

interface Props {
  type: string;
}
const { type } = Astro.props;
const name = typeNameById.get(type) ?? type;
---

<span class="badge badge-type" style={`background:${typeColor(type)}`}>{name}</span>
